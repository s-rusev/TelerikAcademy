gameObjects.Asteroid = function(x, y) {
    gameObjects.GameObject.call(this, x, y, 64, 64, true, true, 2);
    this.element.style.backgroundImage = "url(images/asteroid.png)";
    this.destroyable = false;
};

gameObjects.Asteroid.inherit(gameObjects.GameObject);


